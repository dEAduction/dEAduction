import data.real.basic

-- Some useful defs involving if-then-else
notation `|`x`|`:= @abs ℝ _ x  -- valeur absolue sur ℝ


-- lemma definition.abs
-- {a: ℝ} : abs a = if a ≤ 0 then -a else a :=
-- begin
--     todo,
-- end

-- lemma definition.min
-- {a b: ℝ} : min a b = if a ≤ b then a else b :=
-- begin
--     todo,
-- end

-- lemma definition.max
-- {a b: ℝ} : max a b = if a ≤ b then b else a :=
-- begin
--     todo,
-- end

